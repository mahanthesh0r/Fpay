<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mahanthesh.fpay">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/CustomTheme">
        <activity android:name=".activity.ConfirmTopupActivity"></activity>
        <activity android:name=".activity.TopUpActivity" />
        <activity android:name=".activity.BiometricActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.CreditCardActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activity.PinSettingsActivity"
            android:label="Settings"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activity.SettingsActivity"
            android:label="Settings"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activity.ProfileActivity"
            android:label="Profile"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activity.QRCodeActivity"
            android:theme="@style/CustomMaterialTheme" />
        <activity android:name=".activity.HomeActivity" />
        <activity
            android:name=".activity.SignupActivity"
            android:theme="@style/CustomTheme" />
        <activity
            android:name=".activity.LoginActivity"
            android:theme="@style/CustomTheme"
            android:windowSoftInputMode="adjustResize|stateVisible" />
        <activity android:name=".activity.MainActivity" />
    </application>

</manifest>